#!/usr/local/bin/cake -d

main
	"Bad pos"
	zalgoline "start world\n\n"
	RecursiveHello(0, 0)
	zalgoline "\n\nend world"


function RecursiveHello	(|int| A, |int| B, |int| Depth=0)
	Depth++
	if Depth > 9 // how?
		app.Quit "Too deep"

	"hello world: $A, $B, $Depth"
	
	if b < 4
		RecursiveHello(A, B+1, depth)
	if a < 4
		RecursiveHello(A+1, B, depth)

