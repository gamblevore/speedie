
							Todo

* VM? 
	* use 256 VM codes? Only a few need all 25 bits.


* constructors should allow code before it... just not property setting.!!!!!! FOR THE NIL FIXES

* Do nil checker!
	* MainArgBasic fucked with basodpias;kldjalskjdnasm,dnas booASLKDalksdalksdjm 
	* display obj.haha.property instead of JB_haha(obj).Property

	* picomsg is now crashing or failing on constructor
		* speedie stops compiling sometimes, from perry
		* pico needs a massive bloat bastard constructor/destructor test!!! like 8 threads bastardly creating/destroying picos!

	* name should be "safe" (needs a "getter" func that works like a setter)

	* upgrade optional returns to real? (if possible)
		* started optional returns must return both values!!

	* function pointers not being tested!
	* also... what about global struct pointers? pointers are assumed not nil... but if its not initialised it will be nil?
		* compiler should know this

	* why is "if !(NewType?)" necessary??? "|| ListName = list!" too
	* selector constructor shouldn't need "?"
	*	con.counterpart|array| <~ result // should be rejected!
	* function SafeEdge (|imagepixel|) should be a nil-tester!
	* helper message.LastAfter (|int|ignoreafter, |ind|) shouldn't need nil self
	* helper message.FindTightest (|int| pos, |bool| named=false, |message|) failing
		if (r < size) or ((rz!=nil) and (pos == rz.position and pos == ch.after)) // smallest

	* a.LinkDepth - b.LinkDepth // didn't get upgraded to int!
* speedie not listing all the constructors on Error()



* SC_Imp_Constructor reffing unnecesarily
* change allocator to use unified allocator?
	* s = message.new(void* layer, a, b, c) // can be 0x1 or memlayer+0x1, or an object
* Constants and globals don't autocomplete nicely



						 Perry Do later

* error count should be listed in GUI
* errors should bubble up the folders
* return in the cmd-d should disappear the thing!
* moving arrow in unseen lines, should jump to 1/3 of visibility... as expected
* colormark unused funcs in editor
	* mouseover errors in editor, should put them in a popup
* "but was found elsewhere" could look outside of mini-search... before going to other projects
* Move To Trash shouldn't be allowed for perry.input? Also for extras it should just be renamed to "Forget Folder"
* Multiple fwd/back funcs per-file would be nice, at least from right-click jumps, cmd-L and other jumps
	* having a func-name per-point is nice
* function jumping should be to a function + a relative line... not bytepos... due to editing of files making things messy.
	* What about decls? perhaps just relative to a node?
* too long text in listview draw as much as we can


						 Speedie Do later

* nicer lookups: fn x (|int| n), .x(max) --> .x(int.max) // assuming max doesn't exist locally.
	* just set a global var... somewhere, and do set .obj or whatever it is that we lookup from in lookupsub, to that var if we can't find anything... it should happen in priority to globals, though.
* "calledfrom" autocomplete needs to include references to funcs
	* calledfrom datatypes too
	* @dot completes to @dotAnyGlobalfunc
* To make "dispatch" clickable, we need to alter MoveToInitFunc, so we have an init func per module per file.
* galaxxies C++ compile issues
* RefOpts. Do it bit by bit
	* get property but only did math
	* mark a few string funcs as not-modifier/disowner/capturer
	* mark entire class
	* fast capture of disowned things. like: r1 <~ r2.pop


						 Perry additions

* add image editor...
* skinning support? .HelloKitty or .emo file endings could reskin the IDE dynamically.
	* game-like IDE. functionality, "fix" using gameplay states. Mini-games that "Restore state". Do after the VM. Like collect items that are code and get unlocked in the right order...  that can't just be commented out.
	* Colorthemes... So a theme takes 2 colors, and the drawstyles can darken, grayen, or chooose between them, or use the alpha. This way, we can change the entire IDE by only changing two colors! Each window can have it's own theme. The system can have it's own theme too, or use the front full-screen window's theme. DrawStyle's color, are only computed now.	
* double clicking |abc| on the || should sel the entire thing, doing on the inside should sel the inside
* Find-replace?
	* syntactic-mode also


* double-paradise-realm??

