

main // main { 
	if app.Yes("crash")
		app.Crash
	GUI.Init
	Perry.Startup
	notifications.ShowTip( file://tips.box )
	return GUI.Run
	

function app.InitColors
	¥.border.edge			<~ vec4(1) 
	¥.modifiedborder.edge	<~ vec4(1, 1, 0.5, 1) 
	¥.bg = ¥.bgCorners[0].rgb
	¥.MenuRowSel.solid		= (0.5, 0.9, 0.5, 0.6)
	¥.Menus.edge			= vec4(1, 1, 1, 0.2).px
	¥.Menus.bg				= (0.106, 0.154, 0.031, 1.0)
	

ignore
	but I love %to~watch you eat cheese
//
//#!/usr/local/bin/spd
//import "GUI"

// todo:
// **window alignment sucks**
	// scaling too for scale2
// forgiveness like tetris has

// score board needs block positioning, and history
// pink cloudy blocks with cute faces? and hearts/rainbows/stars as they disappear
	// Clouds via overlapping volumetric spheres and edge detection post-processing
	// frilly frames?

//main
//	gui.Init(2.0)
//	return TetrisWin()
