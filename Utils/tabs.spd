#!/usr/local/bin/spd

app.usage = "Simple utility to convert tabs to spaces.

Pass a string or a file. The file-path must not contain tabs, otherwise it is considered a string."

main (|string| data)
	ifn data contains '\t'
		|| f = data.file
		if f.Exists
			printline f.ReadAll.TabsToSpaces
			return 0
	printline data.TabsToSpaces

