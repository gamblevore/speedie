#!/usr/local/bin/spd


main (|file.existing| A, |file.existing| B)
	|| StrA = A.ReadAll
	|| StrB = B.ReadAll
	
	|| n = stra.Length max strb.Length
	for i in n
		if stra[i] != strb[i]
			"File different at: $i"
			exit

