#!/usr/local/bin/spd

main (|faststring| output, |int| reps, |file| input) // useful for testing speed of parsing
	|| data = input.ReadAll$
	for reps
		output <~ data
	output.Flush

	|| f = output.file	
	"$f is ${f.size.strsize}"

