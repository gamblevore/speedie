#!/usr/local/bin/spd


main (|File.Existing| FileToShrink, |int| Amount)
	require FileToShrink.open
	
	|| OldSize = FileToShrink.size
	if OldSize <= Amount
		"File is already <= $Amount"
	  else
		FileToShrink.size = amount
		"Truncated $OldSize to ${FileToShrink.size}"
	FileToShrink.close
