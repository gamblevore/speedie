#!/usr/local/bin/speedie

app.usage = "Takes a text file of lines, and sorts it, then replaces the original."
main (|file.existing| TextFile)
	|| d = TextFile.ReadAll
		TextFile.Data = d.Sort
		if StdErr.ok
			"sorted: ${d.length.strsize}"