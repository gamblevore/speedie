

function SCFunction.StatelessAccumulate
	opt norefcounts
	require self is cpp
	|| c = .cls
	require c and c is Stateless
	for p in .args
		require p.Type is Stateless
	// is Stateless
	// what about the code inside?
	// this assumes that files are stateless? no good!

	// BUIULD UP THIS ENTIRE SYSTEM LATER!!
	// building blocks (glass, lair, and wator)

