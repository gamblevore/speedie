

module Stability
	|| LastID = 1
	
	function StableStart (|File| f, |faststring|)
		if options.UpdateStability
			rz = faststring()
			if !f.Size
				rz <~ "// This file is not stable till 'release'."
			rz <~ "\n"
			rz.file = f


	function StableEnd (|faststring| fs)
		if fs and fs.Length > 1
			fs <~ "\n"
			fs.flush


	function StabliseFuncs (|bool|)
		|| frogs = packmaker.PackGlobSize
		|| funcs = Compiler.library["stable_id_bank.txt"]
		if funcs.Open(true)
			.UseFuncBank(funcs)
			|| fs_fn = .StableStart(funcs)
			.FillMissingLibFuncs(fs_fn, frogs)
			.StableEnd(fs_fn)
			rz = true


	// todo:
		// ignore roles for pack-gen
	
	
	function BuildLibGlobTable (|message| T, |dictionary of scdecl| table = dictionary())
		opt norefcounts
		for d in PackMaker.LibGlobs
			table[d.ExportName] = d
			if d.ExportPosition // hmmm... shouldn't happen

		for row in T
			|| d = table[row.name]
				|| pos = row[@num]
					d.ExportPosition = pos.int	
	
	
	function DumpGlobs (|file| f, |faststring| fs)
		opt norefcounts
		for d in PackMaker.LibGlobs
			fs <~ "${d.exportname} ${D.exportposition}\n"
		f.SmartData = fs
	
	
	function StabliseGlobs
		|| globs = Compiler.library["stable_glob_bank.txt"]
		|| str = globs.readall(64K)
/		.BuildLibGlobTable(str.parse)			// fill table with old values
/		packmaker.LibGlobs.Sort(SimplestFirst)	// sort them so empty values come after
/		packmaker._LibGlobSize = packmaker.GlobalsSize(packmaker.libglobs) // rebuild size
		.DumpGlobs(globs, FastString())


	function FillMissingLibFuncs (|faststring?| fs, |int| frogs)
		opt norefcounts		
		|&&nil| Table		// just pass 4096. Saves all the fücking around with calculating sizes.
		if platform.isdebug
			Table = jb_vm.vm.InitTable(4096, frogs)

		for fn in Compiler.FuncList
			if fn.NeedsID
				fn.MakeID(fs, table)
		
	
	function scfunction.NeedsID (|bool|)
		require !.IsMacro and .IsLibrary
		require self isnt disabled and AlwaysInline
		require self|scnode| isnt private
		return true
	
		
	function scfunction.MakeID (|faststring| fs, |?&&nil| table)
		|| id = .PackID
		if !id
			if fs
				if fs.Length
					fs <~ "\n"
				fs <~ .ExportName
			id = Stability.LastID++
			.PackID = id
		
		if table
			|| fn = jb_vm.Load(.exportname) // an actual function pointer for testing
				Table[id+31] = fn
//				"id: ${id}(${id+31}) = $.exportname"
	
	function UseFuncBank (|file| bank)
		|| data = bank.ReadAll(1MB)
			|| ID = LastID
			for str.lines in data
				id++
				|| fn = Compiler.ExportNames[str]
				if fn isa SCFunction
					check (!fn.PackID) (str + " replicated ID.")
					fn.PackID = id
			LastID = ID	
		
