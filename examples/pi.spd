#!/usr/local/bin/spd

main
	while
		"Please enter a number:"
		|| s = app.input #loop
		|| input = s.Float
		if StdErr.ok
			"$input * π = ${input*π}" 
		  else
			StdErr.ShellPrintErrors
			StdErr.clear
