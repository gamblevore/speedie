#!/usr/local/bin/spd

main (|[existingfile]| files)
	CompressionStats.All.Live = true
	for f in files
		|| d = f.ReadAll#loop
		|| s = d.parse
			|| hol = s.hollyencode
			|| mz = d.compress
			f.AddExt("hol") <~ hol
			"$f: ${f.size.strsize}
	hol: ${hol.strsize}
	µz: ${mz.strsize}
"
